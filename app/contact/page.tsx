import { Page, SectionContainer } from "@/components/basic";

export default function ContactPage () {
    return (
        <Page>
            <SectionContainer>
                Test
            </SectionContainer>
        </Page>
    );
}